สวมบทบาทเป็นผู้เชี่ยวชาญด้านการออกแบบฐานข้อมูล PostgreSQL (Expert Database Architect) งานของคุณคือการออกแบบโครงสร้างฐานข้อมูล (Database Schema) ที่แข็งแกร่ง รองรับการขยายตัว (Scalable) และถูกต้องตามหลัก Normalization สำหรับ "ระบบจัดการโรงแรมและรีสอร์ตขนาดเล็กแบบ Multi-Tenant" โดยอิงจากความต้องการดังต่อไปนี้

**ภาพรวมของระบบและข้อกำหนดหลัก (Key Requirements):**
1.  **สถาปัตยกรรมแบบ Multi-Tenant:** ระบบต้องรองรับการใช้งานหลายโรงแรม/หลายรีสอร์ต (Tenants) ภายในฐานข้อมูลเดียวกัน
2.  **ระบบจัดการสิทธิ์ผู้ใช้งาน (Role-Based Access Control - RBAC):**
    *   `SuperAdmin`: สามารถจัดการ สร้าง และตรวจสอบข้อมูลของทุกโรงแรมในระบบได้ทั้งหมด
    *   `Admin` (เจ้าของ/ผู้จัดการโรงแรม): สามารถจัดการและมองเห็นได้เฉพาะข้อมูลของโรงแรมตัวเองเท่านั้น
    *   `Staff` (เช่น พนักงานแม่บ้าน): มีสิทธิ์เข้าถึงจำกัด (เช่น ดูและอัปเดตสถานะการทำความสะอาดห้องพักเท่านั้น)
3.  **ระบบจัดการเนื้อหาหน้าเว็บ (Landing Page CMS):** เว็บไซต์หน้าบ้านของแต่ละโรงแรมต้องปรับแต่งได้โดย Admin ของโรงแรมนั้นๆ ซึ่งประกอบด้วย:
    *   รูปภาพ (Hero images, รูปแกลเลอรี) - **หมายเหตุ:** ให้เก็บข้อมูลรูปภาพเป็น URL หรือ Path ของไฟล์เท่านั้น
    *   เนื้อหาข้อความ (รายละเอียดโรงแรม, ข้อมูลติดต่อ, นโยบาย)
    *   หน้า Tab หรือหน้าเพจแบบไดนามิก (เช่น หน้าแนะนำสิ่งอำนวยความสะดวก)
    *   ข้อมูลแนะนำสถานที่เที่ยวท้องถิ่น (ชื่อสถานที่, รูปภาพ, รายละเอียด, ระยะทาง)
4.  **ระบบปฏิบัติการหลักของโรงแรม (Core Hotel Operations):**
    *   **ห้องพักและประเภทห้อง (Rooms & Types):** จัดการประเภทห้อง (เช่น Standard, Villa) และข้อมูลห้องพักจริง (Physical rooms เช่น ห้อง 101, 102)
    *   **ระบบการจอง (Booking Engine):** จัดการการจอง (ทั้งจากหน้าเว็บและ Walk-in), วันที่เช็คอิน-เช็คเอาท์, ข้อมูลผู้เข้าพัก, และสถานะการจอง (รอยืนยัน, ยืนยันแล้ว, ยกเลิก)
    *   **ราคาและฤดูกาล (Pricing):** รองรับการตั้งราคาที่ยืดหยุ่น (ราคาหน้าเทศกาล, วันหยุด เสาร์-อาทิตย์, High/Low Season)
    *   **การชำระเงินและสลิป (Payments):** บันทึกประวัติการชำระเงิน, เก็บรูปสลิปโอนเงิน (เป็น URL/Path), และสถานะการตรวจสอบสลิป (Verify status)
    *   **การจัดการแม่บ้าน (Housekeeping):** ติดตามสถานะความสะอาดของห้องพักแต่ละห้อง (ว่าง/สะอาด, ยังไม่ได้ทำความสะอาด, กำลังซ่อมบำรุง)
    *   **บันทึกรายจ่าย (Expense Tracking):** ระบบบันทึกรายจ่ายประจำวันแบบง่ายๆ (จำนวนเงิน, หมวดหมู่, วันที่, หมายเหตุ)
    *   **ระบบลูกค้า (Customers CRM):** เก็บประวัติผู้เข้าพักพื้นฐาน (ชื่อ, เบอร์โทรศัพท์, LINE ID)

**สิ่งที่คุณต้องส่งมอบ (Required Deliverables):**

1.  **ภาพรวม Entity-Relationship (ER Overview):** สรุปรายชื่อตารางหลักและจัดกลุ่มให้เข้าใจง่าย (เช่น กลุ่ม Auth/Users, กลุ่ม CMS, กลุ่มระบบจัดการโรงแรม, กลุ่มการเงิน)
2.  **PostgreSQL Schema (SQL DDL):** เขียนคำสั่ง `CREATE TABLE` แบบสมบูรณ์
    *   **การจัดการรูปภาพ (Image Storage):** สำหรับฟิลด์ที่เกี่ยวกับรูปภาพทั้งหมด ห้ามเก็บเป็น BLOB หรือ bytea โดยเด็ดขาด ให้ออกแบบคอลัมน์โดยใช้ `VARCHAR` หรือ `TEXT` เพื่อเก็บข้อมูลเป็น URL (เช่น https://...) หรือ File Path จากโปรเจกต์ (เช่น /uploads/...)
    *   ใช้ Data Types ที่เหมาะสม (เช่น ใช้ `UUID` เป็น Primary Key, ใช้ `TIMESTAMPTZ` สำหรับวันที่และเวลา, และพิจารณาใช้ `JSONB` สำหรับเนื้อหา CMS ที่ต้องการความยืดหยุ่นสูง)
    *   ใส่ข้อกำหนด `FOREIGN KEY` เพื่อรักษาความถูกต้องของข้อมูล (Referential Integrity)
    *   **ข้อบังคับสำคัญ:** ทุกตารางที่เก็บข้อมูลเฉพาะเจาะจงของแต่ละโรงแรม จะต้องมี Foreign Key ชื่อ `hotel_id` เพื่อรองรับการทำ Multi-Tenant และ Row-Level Security (RLS) ยกเว้นตารางระบบส่วนกลางอย่าง `users` หรือตารางหลัก `hotels`
3.  **การทำ Index:** แนะนำการสร้าง Index ที่สำคัญเพื่อเพิ่มประสิทธิภาพการค้นหา (เช่น สร้าง Index บนวันที่จอง, หรือคู่ของ `hotel_id` + `status`)
4.  **คำอธิบาย (Explanation):** อธิบายแนวคิดการออกแบบสั้นๆ โดยเฉพาะวิธีที่คุณจัดการเรื่อง Multi-Tenant, วิธีออกแบบโครงสร้าง CMS, และรูปแบบการเก็บข้อมูลภาพ (URL/Path)

**ข้อกำหนดรูปแบบการตอบ:**
*   ใช้ Syntax มาตรฐานของ PostgreSQL
*   นำเสนอ SQL Code ในรูปแบบ Code Block ที่อ่านง่ายและมี Comment อธิบายชัดเจน